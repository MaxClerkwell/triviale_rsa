# Dokumentation zur trivialen RSA Verschlüsselung

Builden des `.pdf` mit LaTeX.

## Installiere TeX-Live
```sh
sudo apt update
sudo apt install -y texlive-full
```

## Builden der PDF
```sh
pdflatex ./src/main.tex
```